#include <stdio.h>
int main (void) {
	int dan, a,b;
	printf("단을 입력해 주세요 : " );
	scanf("%d", &dan);
	for ( a = 1 ; a <= 9 ; a++ ) {
		printf(" %d * %d = %d \n", dan, a, dan*a );
		printf("다시하시겠습니까? (y/n)");
		scanf();
		if (b = 121) {
			main ( );
		}
		else if (b = 110) {
			return 0;
		}
	}
}