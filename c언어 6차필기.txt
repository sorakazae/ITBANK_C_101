#if 1 또는 0
                컴파일을 하거나 하지 안게 함 /*  */ 와 비슷
#endif

함수 안에 만드는 변수         지역변수(각각의 함수만 보이고 사용할 수 있음)
함수 밖에 만드는 변수         전역변수(모든 영역 모든 함수에서 보고 사용할 수 있음)(전역변수는 변수를 만드면 쓰레기 값이 사라지고 0 또는 NULL로 자동으로 초기화 됨)
                                     (프로그램이 시작할 때 부터 만들어지며 프로그램 끝날 때 까지 사라지지 않는다.)
다른 프로그램도 쓸 수 있는 변수       외부변수(전역변수의 특징을 가지고 있다.)

register변수는 주소값이 없다. cpu에 만들어진 변수

배열은 프로그램을 실행 할 때부터 크기가 정해져 있어야 함
        int x;
	scanf("%d", &x);     이거 안됨
	int k[x];

선택정렬      거품정렬


그림 그리고 프로그램 짜기


 행 열
 
 줄 칸
                     순으로 읽고 사용함
 [2][3]

세로 가로

1차원 배열 먼저 보고 2차원 배열 보기

3차원 배열 신경쓰지 않기
